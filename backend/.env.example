# Sunucu
PORT=5000
NODE_ENV=production

# MongoDB (Atlas bağlantısı)
MONGODB_URI=mongodb+srv://kullanici:sifre@cluster.mongodb.net/campusgame

# JWT
JWT_SECRET=en_az_32_karakter_cok_guclu_bir_secret_buraya_yaz

# ─── E-POSTA DOĞRULAMA (opsiyonel) ───────────────────────
# Boş bırakılırsa doğrulama kodu göndermez, direkt kayıt yapar

# Sağlayıcı: gmail | outlook | smtp
MAIL_PROVIDER=gmail

# Gmail için: Ayarlar → Güvenlik → 2 Adımlı Doğrulama AÇ → Uygulama Şifresi oluştur
MAIL_USER=kampusquest@gmail.com
MAIL_PASS=abcd efgh ijkl mnop   # 16 haneli uygulama şifresi (boşluksuz yaz)
MAIL_FROM_NAME=KampüsQuest MKÜ

# Özel SMTP için (MAIL_PROVIDER=smtp)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
