# API ve Socket URL'leri boş bırakılırsa window.location.hostname ile otomatik bulunur
# Aynı makineden kullanıyorsanız: REACT_APP_BACKEND_PORT=5000 yeterli
# Farklı IP'den erişmek için burayı DEĞİŞTİRMENİZE GEREK YOK — otomatik çalışır
REACT_APP_BACKEND_PORT=5000
